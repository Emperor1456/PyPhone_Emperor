# ♻️ Spaced Repetition – Review Module 02 (Control Flow & Loops)

Before starting Module 03, ensure you can solve these tasks **without hints**.

## 🧪 Consolidation Task
Re‑run the **Hard** level of every practice sheet from Module 02.  
Then answer:
- Can I write multi‑branch `if-elif-else` and nested conditionals?
- Can I use `match-case` for pattern matching?
- Can I write `while` and `for` loops fluently?
- Can I control loops with `break`, `continue`, and `pass`?
- Can I convert a `for` loop into a `while` loop and vice versa?

## 📋 Lessons Reviewed
- L‑14: Basic `if` Statement
- L‑15: `if-else`
- L‑16: `if-elif-else` Chain
- L‑17: Nested Conditionals
- L‑18: `match-case` (Pattern Matching)
- L‑19: `while` Loop
- L‑20: `for` Loop
- L‑21: `break` and `continue`
- L‑22: `pass` Statement
