# 🔁 Module 02 – Final Review (Control Flow & Loops)

You have completed all lessons on conditionals and loops.  
Consolidate your mastery with the tasks below.

## 🧪 Consolidation Task
Re‑run the **Hard** level of every practice sheet in this module **without hints**.  
Then answer these questions:

- Can I write a multi‑branch `if-elif-else` without thinking?
- Can I convert a `for` loop into a `while` loop and vice versa?
- Can I explain when `break` and `continue` are appropriate?
- Can I read and debug nested conditionals?

## 📋 Lessons Covered
- L‑14: Basic `if` Statement
- L‑15: `if-else`
- L‑16: `if-elif-else` Chain
- L‑17: Nested Conditionals
- L‑18: `match-case` (Pattern Matching)
- L‑19: `while` Loop
- L‑20: `for` Loop
- L‑21: `break` and `continue`
- L‑22: `pass` Statement
